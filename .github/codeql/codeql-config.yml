name: "Code Quality Checks"

disable-default-queries: true

packs:
  javascript:
    - codeql/javascript-queries

  actions:
    - codeql/actions-queries

queries:
  - uses: javascript-code-quality-extended.qls